{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Renderer",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}\\executable\\${command:cmake.buildType}",
        }
    ]
}
